# USER_TODO — User requests

> Write what you want the AI to do. The autonomous cycle reads this, generates tasks, then clears it.

- Add a login screen with email + password, validating inputs and showing errors.
- Wire it to the existing auth API and handle the error responses.
